菜单管理 	：修改 树形结构的时候 递归修改(已解决)
		修改完成之后 判断是否有菜单是他的子菜单 如果有查询出来遍历 在遍历里面递归调用本方法
RBAC	:  修改 树形结构的时候 递归修改(已解决)
		修改树形角色 子角色继承父角色的菜单权限（）







RBAC 增加的时候 父级只能是 自己或者自己的子角色（解决）        权限分配的时候 只能是当前角色已拥有的权限（已解决）
	解决方案:得到当前角色下的子角色 - 对应id的子角色	解决方案	:id!=0 的时候 根据id 查询他的父级角色 然后渲染父级角色拥有的菜单权限
		=上级列表	（除了当前id角色及子角色）		当id==0的时候 根据当前登录用户的角色id 查询拥有的菜单权限
    用户访问控制(authmanager);完成
    菜单渲染(未解决);



